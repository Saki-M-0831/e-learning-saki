<h1 class="text-center my-4">Administrators</h1>

<section class="row">
    <% @users.each do |user| %>
        <div class="col-6 mx-5">
            <div class="card mb-2">
                <div class="card-body">    
                    <div class="row">
                        <div class="col-3 align-center">
                            <% if user.picture? %>
                                <%= image_tag user.picture.url, class: "rounded-circle", :size => "100x100" %>
                            <% else %>
                                <%= image_tag "https://cdn.pixabay.com/photo/2017/01/10/03/54/icon-1968236__480.png", class: "rounded-circle", :size => "100x100" %>
                            <% end %>
                        </div>
                        <div class="col-6 mt-4">
                            <h5>
                                <% if user.is_admin == true %>
                                    <i class="fas fa-crown text-warning"></i>
                                <% end %>
                                <%= link_to user.name, root_path %> 
                            </h5>
                            <%= user.email %> <br>
                        </div>
                        <div class="col-3 mt-4">
                            <% if user.is_admin == true %>
                                <%= link_to "Remove From Admin", root_path, class: "btn btn-outline-danger my-2" %>
                            <% else %>
                                <%= link_to "Set As Admin", root_path, class: "btn btn-outline-info my-2" %>
                            <% end %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    <% end %>
</section>
